// vite.config.ts
import { defineConfig } from 'vite';
import { svelte } from '@sveltejs/vite-plugin-svelte';
import { VitePWA } from 'vite-plugin-pwa';

export default defineConfig({
  base: '/ChinesischApp/',
  plugins: [
    svelte(),
    VitePWA({
      registerType: 'autoUpdate',
      strategies: 'generateSW',
      // KEIN manifest:{} hier – wir nutzen das statische public/manifest.webmanifest
      includeAssets: ['favicon.svg', 'robots.txt', 'apple-touch-icon.png'],
      workbox: {
        navigateFallback: '/ChinesischApp/index.html',
        navigateFallbackDenylist: [/^\/ChinesischApp\/assets\//, /^\/ChinesischApp\/icons\//],
        globDirectory: 'dist',
        globPatterns: ['**/*.{js,css,html,webmanifest,png,svg,ico}']
      }
    })
  ]
});
